import path from 'path';
import fs from 'fs';

/**
 * Read the profile/bio of each member from '.all-members' file
 * to avoid over-fetching from GitHub
 */
export function getAllMembers() {
  // const membersResourcePath = path.
}
